/**
 * Access credentials — GENERATED FILE. Do not edit by hand.
 *
 * Regenerate with:  npm run create-user
 * (copy config/users.example.json to config/users.json first)
 *
 * `users: []` means the access gate is INACTIVE and the deck is readable by
 * anyone who has the URL. That is the deliberate default for a repository,
 * because a committed credential is worse than no credential.
 *
 * Read docs/SECURITY_LIMITATIONS.md before treating this as protection.
 */
window.FM_AUTH = {
  iterations: 310000,
  salt: '',
  users: [],
};
